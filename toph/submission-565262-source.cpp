#include <bits/stdc++.h>
using namespace std;



int main()
{
    int t;
    cin>>t;
    for (int i = 1; i <= t; ++i) {
        int n, m;
        cin >> n >> m;
        int s, e;
        cin >> s >> e;
        for (int k = 0; k < m; ++k) {
            int a, b;
            cin >> a >> b;
        }

        cout << "Case " << i << ": " << 1 << endl;
    }
    return 0;
}